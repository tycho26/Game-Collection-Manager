import data from "../settings.json"

const SETTINGS = {
    "Server IP": data["Server IP"],
    "Server port": data["Server port"],
    "First time": data["First time use"]
}

export default SETTINGS;